function checkUser(user, pass)
    --TODO: Andrei: Use the service from Vasile
    --to check the user.
    return true
end

function checkService(service_key, pass)
    --TODO: ANdrei: User the service from Vasile
    --to check the service credentials.
    return true
end
